- 使用 codegraph MCP检索
- 请调用智能体和Worktree并行工作 确保阅读过项目所有md
- 维护项目的所有md文档，有些文档内容可能过时要分辨
- 要求代码里每步都要中文注释功能的实现和实现的原因，为后期排查问题和开发做好基础
- 按照项目代码功能结构,功能区域划分规则，进行开发修改，不要擅自改变代码架构和功能划分结构。
- 你先规划架构，不明白的细节可以先和我提问确认，再写代码，你开发阶段可以打开 无头 chrome 访问主站页面调试验证。
- 每次写代码前先给"改动前总结"，写完后给"改动后总结
- 每次更改变动要按照格式中文写入项目对应根目录下的AGENTS.md CHANGELOG.md DEPLOY_CREDENTIALS.md PROJECT_PLAN.md记录写入北京时间和日期，方便后期再开发修改的时候快速定位
- 把在本项目需要长期记住的开发提示，写到本文件的下方，记录写入北京时间和日期
- 最小化侵入，不用改原始项目 后期好同步源作者代码 只在 vscode-extension/ 目录里面修改

---
开发提示如下：- [2026-08-22 17:30:00] vscode-extension 修复 VS Code 原生 MCP 每次打开不自动启动（0.9.33）。根因：Copilot Chat `chat.mcp.autostart` 默认 `onlyNew`（仅"从未运行过"的 MCP 服务器首次提交聊天时自动启动一次），重开 VS Code 后已配置但未运行的 codegraph 不会自动拉起。修复：`agentConfig.ts` 新增 `ensureVSCodeMcpAutostart()`——用 `workspace.getConfiguration('chat.mcp').inspect('autostart')` 检测用户从未显式设置（globalValue/workspaceValue/workspaceFolderValue 均 undefined）时写入 `newAndOutdated`；显式设置过的值一律尊重。注意 inspect 的字段名是 `workspaceFolderValue` 不是 `folderValue`（TS 编译报错教训）。仅在 VS Code 代理配置成功后调用，且 `chat.mcp.access != none` 时才写。其他代理排查：opencode/Kilo 自带 enabled:true，其余 stdio MCP 随主进程拉起，无此问题。版本 0.9.32 -> 0.9.33。- [2026-08-17 00:35:00] vscode-extension 新维度审查（子进程交互协议）修复「建立索引」永久挂起（0.9.32，严重）。根因链：`codegraph init` 索引完成后调 `offerWatchFallback` -> git 仓库且 watch 禁用（WSL2 `/mnt/*`、`CODEGRAPH_NO_WATCH=1`）时弹 `clack.select`；扩展 `runCliCommand` 用管道 stdin（非 TTY）spawn，**clack.select 在非 TTY stdin 下永久挂起**（实测）。修复要点（两层缺一不可）：① 3s 后 `stdin.write('\r')` 让 select 接受默认推荐项（装 git hooks）；② `stdin.end()` 模拟 EOF 让进程退出--只写回车不关 stdin，流程走完后进程仍挂（clack keypress 监听保持事件循环活跃）。另加 120s 总超时 SIGKILL。教训：**spawn 交互式 CLI 必须考虑非 TTY 行为；shell 管道 `(sleep;printf)` 测试会自动产生 EOF，Node pipe stdin 不会--验证时必须用 Node 复刻而非 shell 管道**。端到端验证模板：/tmp git 仓库 + CODEGRAPH_NO_WATCH=1 + pipe stdin。版本 0.9.31 -> 0.9.32。- [2026-08-17 00:10:00] vscode-extension 交叉重新审查修复 3 个 bug（0.9.31）。① **configureKiloCode 数据丢失遗漏**：`kilo.jsonc` 是 JSONC 格式却用 `readJsonConfig`（JSON.parse）读，失败返回 `{}` 覆盖清空——与 0.9.30 修的 opencode 同模式，当时只修了 opencode。② **readJsonConfig 通用损坏保护**：解析失败从返回 `{}` 改为返回 `null`（文件不存在/空白仍返回 `{}` 支持新建），12 处代理配置调用点全部加 `if (config === null) return configReadError(path)` 放弃写入；教训：JSONC 格式的代理配置有 opencode（`opencode.jsonc`）与 Kilo Code（`kilo.jsonc`）两个，改数据保护类 bug 时要 grep 全部 `.jsonc` 使用者，勿只修一个。③ **McpClient.stop() SIGKILL 兜底失效**：`proc.killed` 表示"kill() 已调用"（SIGTERM 后立即 true）而非"进程已死"，子进程忽略 SIGTERM 时 `if (!proc.killed)` 恒 false，SIGKILL 永不发出；存活判断改用 `proc.exitCode === null && proc.signalCode === null`。另：所有 configure 失败路径 action 从 'created' 统一为 'unchanged'。版本 0.9.30 -> 0.9.31。
- [2026-08-16 23:45:00] vscode-extension 代码审查修复 12 个 bug（0.9.30）。① configureOpencode 数据保护（最严重）：opencode.jsonc 是 JSONC 格式，`readJsonConfig`（JSON.parse）解析失败返回 `{}`，旧逻辑会 JSON.stringify 整体覆盖写入清空用户配置；新增零依赖 `stripJsonc()`（单遍状态机剥离注释/尾逗号，字符串内不误伤）与 `readJsoncConfig()`（解析失败返回 null → configureOpencode 放弃写入返回 success:false）。注意 `.vscodeignore` 排除 `node_modules/**`，打包后不能 require('jsonc-parser')，必须零依赖。② 状态栏重试失效：`startCodeGraph` 拆分为公开入口（重置 retryCount）+ 内部 `doStartCodeGraph()`（自动重试递归不重置），`handleStartError` 边界 `<` 改 `<=`（否则第 3 次 8s 重试永远不触发）。③ Windows `findCodeGraphCommand` 的 `where codegraph` 多行输出取 `split(/\r?\n/)[0]`，否则多行整体 existsSync 恒 false 误判未安装。④ commands.ts 同名符号匹配：`parsed.find(name===label)` 恒取第一个，改用 `items.indexOf(selected)` 索引映射（showQuickPick 返回原数组元素引用）。⑤ McpClient 握手 clientInfo.version 硬编码 0.9.9，改构造函数注入 `context.extension.packageJSON.version`。⑥ onCrash 后补 `setContext('codegraph:ready', false)`（命令面板 when 依赖）。⑦ 两个安装器加 120s 超时 SIGKILL。⑧ 删 `No ${displayName.toLowerCase()} found` 死代码（displayName 中文恒 false）。⑨ treeProvider `createErrorNode` 固定 id 'tree-error' 冲突，CodeGraphTreeNode.id 改可选。⑩ configureClaudeCode 权限补齐与 mcpServers 变更解耦（无条件幂等补权限）。⑪ search-root 死功能：改 None + command 触发 codegraph.searchSymbol。⑫ configureHermes command 用 codegraphPath 绝对路径（JSON.stringify 生成 YAML 双引号值防空格/反斜杠），未提供路径时保持裸命令保证幂等。版本 0.9.29 -> 0.9.30。
- [2026-08-16 23:28:00] vscode-extension 修复 VS Code Chat MCP 服务器 spawn ENOENT（0.9.29）。根因：VS Code Server（Remote-SSH/Agent Host）进程 PATH 不含 `~/.local/bin`（shell 配置仅交互/登录 shell 生效），MCP 配置 command 用裸 `codegraph` 时 spawn 失败。修复：`configureVSCode(codegraphPath?)` 用绝对路径（`findCodeGraphCommand()` 结果）作 command 写入 mcp.json + `~/.copilot/mcp-config.json`；`AgentConfig.configure` 接口加可选 `codegraphPath` 参数，`configureAgents` 透传（其它代理仍裸命令）。注意：VS Code 原生 MCP 由 VS Code Server 进程启动（非扩展子进程），不吃扩展的 buildSpawnEnv()，必须写绝对路径。版本 0.9.28 -> 0.9.29。
- [2026-08-16 23:09:50] vscode-extension 修复 Remote-SSH/Agent Host 下 VS Code MCP 配置不生效（0.9.28）。关键：Agent Host 只读 `~/.copilot/mcp-config.json`（用户级）与 `.mcp.json`（工作区），不读 `.vscode/mcp.json`；远程形态（Remote-SSH）下用户级 mcp.json 在 `~/.vscode-server/data/User/mcp.json`（即 `MCP: Open Remote User Configuration` 的文件），不是 `~/.config/Code/User/mcp.json`（本机桌面版目录）。`getVSCodeConfigPath()` 优先 vscode-server userData、`configureVSCode()` 额外写 `~/.copilot/mcp-config.json`（目录存在时）、`isVSCodeInstalled()` 加 `~/.vscode-server/data/User`+`~/.copilot` 检测。版本 0.9.27 -> 0.9.28。
- [2026-08-16 22:46:05] vscode-extension 新增 VS Code 原生 MCP 自动配置（0.9.27）。用户级配置路径 `{userData}/User/mcp.json`（Linux `~/.config/Code|Code - Insiders/User/mcp.json`、macOS `~/Library/Application Support/...`、Windows `%APPDATA%\...`），新增 `getVSCodeConfigPath()` 自动解析稳定版+Insiders 两种发行形态（优先已存在 mcp.json → 父目录 → 兜底稳定版）、`configureVSCode()` 写 VS Code 专属的顶层 `servers` 键（区别于 Cursor/Claude 的 `mcpServers`，值结构复用 `MCP_SERVER_CONFIG`）、`isVSCodeInstalled()` 检测各平台 userData 目录。i18n noAgents/configuring 文案加 VS Code。配置后需重启 VS Code 让 Copilot Chat 发现 MCP 服务器。仅改 vscode-extension/，未动上游 src/。版本 0.9.26 -> 0.9.27。
- [2000-06-22 18:59:00] 演示条目
- [2026-08-02 03:59:57] vscode-extension 新增 Trae IDE 自动 MCP 配置。Trae 有两种发行形态：SOLO/服务端用 `~/.trae-server/data/Machine/mcp.json`（本机实跑形态），标准桌面版用平台相关 `Trae/User/mcp.json`。新增 `getTraeConfigPath()` 自动解析路径、`configureTrae()` 写 mcpServers.codegraph、`isTraeInstalled()` 检测 `~/.trae`/`~/.trae-server`/平台桌面目录。仅改 vscode-extension/，未动上游 src/。版本 0.9.22 -> 0.9.23。
- [2026-08-02 04:26:01] vscode-extension 代码审查修复 7 个 bug（0.9.24）。Fix#4 mcpClient close 处理器补 else 兜底，防 ready 前 code=0 退出致 start() 永久挂起；Fix#7 configureHermes 改 upsert 就地替换 codegraph 块，防非标准 command 时追加重复 mcp_servers YAML；Fix#2 getSymbolAtCursor 修复限定名（PascalCase 接收者返完整限定名、光标在接收者返裸词、变量接收者返末段）；Fix#1 promptInitialize 改用 i18n 死键 prompt.initQuestion；Fix#3 parseFilePaths 去掉过激过滤（保留 Makefile 等无扩展名根文件）；Fix#5 configureAgentsManual 去重复通知；Fix#6 onCrash 加 signal 参数修复信号名丢失。注意：commands.ts/agentConfig.ts 含 em-dash（U+2014）与行尾空格，编辑时需精确匹配。
- [2026-08-02 05:13:07] vscode-extension 新增 Trae CN IDE 自动 MCP 配置（0.9.25）。Trae CN 目录约定与 Trae IDE 对称：`.trae-cn`+`.trae-cn-server`，SOLO 配置路径 `~/.trae-cn-server/data/Machine/mcp.json`（`~/.trae-cn/` 为运行时资源不放 mcp 配置）。桌面版三平台 `Trae CN/User/mcp.json`。新增 `getTraeCnConfigPath()`/`configureTraeCn()`/`isTraeCnInstalled()`，与 Trae IDE 独立配置可共存。i18n noAgents 文案加 Trae CN。仅改 vscode-extension/，未动上游 src/。版本 0.9.24 -> 0.9.25。
- [2026-08-02 13:56:05] vscode-extension 变更记录与代码注释文案精简（0.9.26）。去除 Trae CN 相关注释与 CHANGELOG 中的地区性/非正式表述，适配扩展商店审核；保留 "Trae CN" 产品名。仅改 vscode-extension/，未动上游 src/。版本 0.9.25 -> 0.9.26。
